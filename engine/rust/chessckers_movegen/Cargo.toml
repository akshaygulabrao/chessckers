[package]
name = "chessckers_movegen"
version = "0.1.0"
edition = "2021"

[lib]
name = "chessckers_movegen"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module", "abi3-py311"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
