# Local Claude Code configuration
/.claude/

.DS_Store

# Training run artifacts (data, not code)
engine/runs/
engine/engine/

# lc0 reference clones (read-only architecture reference, not our code)
reference/
